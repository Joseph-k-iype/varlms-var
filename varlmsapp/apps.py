from django.apps import AppConfig


class VarlmsappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'varlmsapp'

    # from django.apps import AppConfig

    # class APP_NAMEConfig(AppConfig):
    #     name = 'APP_NAME'
    #     icon = 'ICON_CLASS'  # for example: icon = 'fa fa-users'